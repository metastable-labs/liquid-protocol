// SPDX-License-Identifier: MIT
pragma solidity ^0.8.12;

contract AerodromeEvents {
    // event LiquidityAdded(
    //     address indexed tokenA, address indexed tokenB, uint256 amountA, uint256 amountB, uint256 liquidity
    // );
    // event LiquidityAddedETH(address indexed token, uint256 amountToken, uint256 amountETH, uint256 liquidity);
    // event LiquidityRemoved(
    //     address indexed tokenA, address indexed tokenB, uint256 amountA, uint256 amountB, uint256 liquidity
    // );
    // event LiquidityRemovedETH(address indexed token, uint256 amountToken, uint256 amountETH, uint256 liquidity);
}
